const sitemapConfig = {
    siteUrl: 'https://sujalchauhan.in', // Replace with your actual domain
    generateRobotsTxt: true,            // (optional) Generates robots.txt file
    exclude: ['/private-page'],         // Exclude specific pages (if any)
}

module.exports = sitemapConfig
